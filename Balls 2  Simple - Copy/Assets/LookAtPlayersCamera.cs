using UnityEngine;
using System.Collections;

public class LookAtPlayersCamera : MonoBehaviour {

	// Update is called once per frame
	void FixedUpdate ()
	{

		//transform.LookAt (transform.root.GetComponent<Attributes> ().m);
	}
}
